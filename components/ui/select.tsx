"use client";

interface SelectProps {
  className?: string;
}

export function Select({ className }: SelectProps) {
  return (
    <div className={className}>
      {/* TODO: implement Select */}
      <span className="text-sm text-muted-foreground">Select (stub)</span>
    </div>
  );
}

export default Select;
