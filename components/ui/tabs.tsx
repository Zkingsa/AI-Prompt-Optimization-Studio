"use client";

interface TabsProps {
  className?: string;
}

export function Tabs({ className }: TabsProps) {
  return (
    <div className={className}>
      {/* TODO: implement Tabs */}
      <span className="text-sm text-muted-foreground">Tabs (stub)</span>
    </div>
  );
}

export default Tabs;
