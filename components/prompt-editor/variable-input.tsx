"use client";

interface VariableInputProps {
  className?: string;
}

export function VariableInput({ className }: VariableInputProps) {
  return (
    <div className={className}>
      {/* TODO: implement VariableInput */}
      <span className="text-sm text-muted-foreground">VariableInput (stub)</span>
    </div>
  );
}

export default VariableInput;
