"use client";

interface BatchRunnerProps {
  className?: string;
}

export function BatchRunner({ className }: BatchRunnerProps) {
  return (
    <div className={className}>
      {/* TODO: implement BatchRunner */}
      <span className="text-sm text-muted-foreground">BatchRunner (stub)</span>
    </div>
  );
}

export default BatchRunner;
